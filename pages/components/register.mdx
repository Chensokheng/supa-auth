# Sign up

import Register from "../../components/ui/auth/register";

export const Layout = () => {
	return (
		<div className="py-10">
			<Register />
		</div>
	);
};

<Layout />

# Installation

```sh copy
npx supaauth@latest add --component register
```

This will do:

1. create `register.tsx`, `signup.tsx`, `social.tsx` files for you
2. It will install all dependencies by running these cmds

```sh
npx shadcn-ui@latest add form
npx shadcn-ui@latest add input
npm i react-icons
```

## Skip dependencies

If you have all dependencies you can skip it by do this

```sh copy
npx supaauth@latest add --component register --skip dep
```
