# Sign In

import SignIn from "../../components/ui/auth/signin";

export const Layout = () => {
	return (
		<div className="py-10">
			<SignIn />
		</div>
	);
};

<Layout />

# Installation

1. install all dependencies from register

```sh copy
npx supaauth@latest add --component register
```

2. install all dependencies from register

```sh copy
npx supaauth@latest add --component signin
```
